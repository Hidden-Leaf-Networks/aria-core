{{/*
Secret template for reference only.
In production, create secrets externally (sealed-secrets, external-secrets, vault).
Use: kubectl create secret generic aria-core-secrets \
  --from-literal=jwt-secret=$(openssl rand -base64 32) \
  --from-literal=postgres-password=$(openssl rand -base64 16)
*/}}
